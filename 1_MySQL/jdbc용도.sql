CREATE TABLE person(
	id INT PRIMARY KEY AUTO_INCREMENT,
	name VARCHAR(20),
    age INT,
    addr VARCHAR(50)
);

SELECT * FROM person; 
CREATE TABLE member(
	id VARCHAR(30) ,
    password VARCHAR(30),
	name VARCHAR(10)
);


SELECT * FROM member;